angular.module("simpleSite",[]);

/*
creating a module. 

angular.module("store",[]);
angular.module create a module. Name of the module, which is aka application name, is store
[] is dependenices that can be declared

angular.module("myApp", [])
*/
